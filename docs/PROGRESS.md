# 开发进度

## 2026-09-10

- 精简首页与高潜趋势页顶部文案：移除“结果档案”导航标签和三处主标题，将“形鉴”统一更名为“用户审美洞察与趋势捕捉”，并调整移动端品牌字号避免拥挤。

## 2026-09-09

- 已新增 `site.sh` 固定域名一键启停脚本，提供 `start`、`stop`、`restart`、`status` 子命令；启动后 FastAPI 仅监听本机，Vite 监听 `0.0.0.0:80`，可通过 `http://gamedevcenter.ahagamecenter.com` 直接访问。
- 已将固定域名加入 Vite Host 白名单与 FastAPI CORS 允许来源；运行时 PID 与日志保存在忽略版本控制的 `.runtime/`。
- 已在部署服务器实测 `http://gamedevcenter.ahagamecenter.com` 返回 HTTP 200，且同域名 `/api/v1/llm/models` 可正确经 Vite 代理访问 FastAPI；当前站点由 `design-ai-site.service` 持续运行，可执行 `./site.sh stop` 停止。

- 按用户要求取消高潜趋势默认输出 token 限制：网页归纳和综合调用均不传 max_tokens，不再套用模型目录的输出上限；Skill 新任务默认输出预算为 null，适配器允许省略此参数。历史网页任务若显式继续，实际调用快照也不带旧上限；不改写既有结果或自动重跑。服务/SDK 默认限制与截断检测仍保留。

- 高潜趋势新增用户范围选项（自动识别 / 全部 / 前 N 位）。预估与创建任务共用代码解析“只选择前 20 个用户”“前二十位用户”等表达，并在 Skill prepare 前传入 user_limit；按原数组顺序选整位用户，再过滤无效回答，不用后续用户补足。文字和选项冲突时提示修改。
- 页面预估、启动确认、历史与任务详情展示实际选中人数和有效文本人数；新任务持久化范围来源、数量和所选用户 ID。旧失败任务保持冻结的 70 位用户范围，继续时不会重新解读提示词。同步修正模型提示，避免对每个资料包重复截取前 N 位。
- 验证使用两位用户的微型资料与假模型：10 项后端测试通过，覆盖真正进入模型的内容、预估/创建一致、冲突、空回答、不补后续用户、超过总人数、参数错误以及原有失败恢复；前端构建通过。未进行真实模型调用。

- 高潜趋势补充可公开的错误类别、失败步骤/尝试序号/耗时及持久错误历史（不回传网关正文或连接信息）。新增手动继续接口与页面入口，复用冻结资料、Skill、模型和已接受笔记，仅重排未完成步骤；累计失败尝试仍占预算，上限不足先拒绝，可显式调整。每次尝试单独保存消息快照，原快照不覆盖。8项微型离线测试通过，包含第二步失败后继续、第一步不重跑、预算不足、完成任务拒绝重跑、重启恢复和错误脱敏；前端构建通过，未调用真实模型。按用户提供的堆栈，为web_b8fa48051b704d85907aad1b20cf3c19补录已核实的stream_read_error，未自动继续该任务。

- 高潜趋势新增聊天式文字输入框（可选，最多4,000字符）：支持补充关注方向、场景和输出要求，预估与确认提交同步携带；每个归纳/汇总阶段均接收同一份要求，实际消息快照、最终执行信息与历史任务保留原要求。字符预算包含该文本，修改文字使旧预估失效。7项微型离线测试和前端构建通过，未调用真实模型。

- 新增网页高潜趋势模块 `/high-trends`：趋势日期范围、共享模型目录、纯代码预估、可调调用上限（默认24）、确认运行、历史任务、2秒轮询阶段/批次、实际调用与已知Token统计、Markdown/JSON下载。结果以主方向和用研补充卡片展示，趋势/用研关联图片分别预览，展开可查看来源原文；不展示未入选文章诊断列表。
- 后端新增 `/api/v1/high-trends`，直接复用3.0 Skill的准备、分包、接收和发布函数。每一步绑定DeepAgents与冻结Skill快照，StateBackend仅提供该Skill文本，模型端禁用文件/委派工具并限制一次推理；完整系统文本预留在48,000字符预算内，无隐式重试和格式修复调用。
- 网页任务和产物持久保存到 `data/result/high_trend/web_<id>/`，包含输入索引、Prompt/Skill快照、阶段日志和用量。单进程只允许一个活动任务；失败保留笔记并停止，重启将未完成任务标为中断，不自动产生新费用。图片API仅暴露结果已关联且位于研究目录内的本地图片，拒绝路径及软链接越界。
- 验证仅用最多2条趋势、2位用户的微型夹具：6项新测试通过，覆盖真实DeepAgents图+假模型API端到端、分包后一次汇总、空范围、日期/模型校验、预算、失败/截断、恢复读取、图片路径边界。另有3项DNA API兼容测试通过；前端生产构建通过；隔离测试服务中完成浏览器预估/启动/进度/来源展开/图片预览检查。没有真实模型API调用，也未运行全量研究；实际模型归纳质量仍需后续小样本实测。

## 2026-09-08

- 按用户“明显重合即可、不要严格审核反证”的要求，将趋势 Skill 默认入口重做为 3.0.0：代码整理原文本 → 按字符预算归纳少量方向笔记 → 一次综合出稿；小资料直接一次成稿。去掉逐条观察提取、排他主题分配、候选筛选、全量反证、分轮合并与逐卡审核，2.x仅留作历史兼容。
- 3.0 默认48,000字符完整请求、总调用上限12次（包含失败重试）、笔记/成稿输出上限2,500/5,000 token。跨用户合包，共享问题/画像；综合只读短笔记，代码管理短来源编号、用户去重、图片路径和轻量JSON。普通Markdown可直接接收，未知编号与缺图记提示，不触发模型纠错；脚本自动推进、复用笔记缓存，减少宿主协调往返。
- 根据用户最新约定，后续流程验证只使用极少量代表性数据，已写入AGENTS.md和Skill。此次16个新离线用例仅使用最多2条趋势、3位用户、约8条回答/需求：验证一次直出和两个笔记包加一次综合、格式容错、缓存、预算及续跑；新增真实模型调用为0，未重跑前20用户或全量资料，不把模拟验证称为实际token/耗时降幅。
- 兼容与分发：另用2个微型离线用例检查旧版显式入口，均通过；Skill规范校验通过。默认ZIP及design-trend-synthesizer-3.0.0.zip包含51个分发文件，逐字节与源码一致，旧2.x ZIP保留。项目与个人Skill链接自动指向当前3.0入口。

- 实测期间为后端趋势适配器补充 `observability.schema_version=1`：使用旁路回调记录首个流事件、首段可见文本及 invoke 结束时间，追加 provider 已报告的缓存输入/推理输出等细分；旧总量、消息、参数、超时及重试保持原样。19 项离线假流与适配器测试通过，新增观测已由后续真实调用自然采样；首段前耗时不能直接解释为纯排队，旧 79 秒案例无法补回分段。
- 前 20 用户实测已生成收敛交付：近两月固定为 2026-07-08 至 2026-09-08，5 条趋势、用户 ID 45–64，1,966 条来源全部完成提取；主题 41 批、候选 63 批完成。用户指出耗时与 token 过高后，停止大输入试验及继续扩张队列，保存 screen 34/56 批，复用既有证据收敛成 2 张主卡与 3 个用研补充方向，输出于 `data/result/high_trend/20260908T092310535168Z_72de0f3e/final_delivery/`。不把收敛交付声明为原八阶段全部完成；原队列未再派发、收尾新增后端调用为 0。
- 本轮交付核验：78 条唯一引用逐字匹配原始输入，两个主卡均有双侧依据；补充方向去重人数为 19/20、18/20、16/20（议题提及而非偏好率）。15 处图片关联中 10 处有路径，对应 6 张不同本地图片；5 处用户图路径缺失保留为空，未读取图片内容。
- 本轮截至用量快照约 303.9 分钟，后端 247 次调用已知 2,146,886 token，33 次缺少用量；候选阶段累计调用约 130.3 分钟、908,328 token，超过提取阶段。宿主与子代理另有 78,834,319 token，其中缓存输入 75,644,800、未缓存输入 2,708,188、输出 481,331，含调试、修复和协调，不等同于纯生产流水线成本或相同费用；详情与逐次留痕见本轮 execution_review.md/json。
- 20 用户实测期间补充 `extract_metadata_v5` 无损长引文分包：仅对已核实的显式连续原文按完整句末拆段，原文字、维度、立场和图片角色全部保留，并记录来源哈希及片段偏移；总观察预算不足或存在其他非法属性时不处理。真实 658 字/33角色回复拆为404字20角色与254字13角色，整批严格验收通过、恢复新增模型调用为0；此前两次失败补齐的4,997 token及107.3秒保留在真实统计中。
- v5 校验：240项包内测试及仓库外禁用第三方包后的240项独立测试均通过，43个分发文件逐字节一致；230个冻结的原始任务、请求、已接收及尝试文件哈希未变。默认ZIP和 `design-trend-synthesizer-2.3.0-normalization.zip` 已同步，原2.3.0 ZIP保留。规范化版本独立升级，模型契约、Prompt和语义缓存键不变。
- 按用户要求优化短回答的输入开销，趋势 Skill 升至 2.3.0：extract 的记录与共享语境均采用列式表格，字段名每批只发送一次，完整相同语境去重，画像按用户去重；保留全部原文字段、类型、归属以及缺失/null区别。来源 ID 在原任务中冻结为短 ID，补齐子集继承原映射，接收后由代码还原，再做原有证据校验；图片编码不参与转换。语义缓存键保持兼容，原暂停运行未推进。
- 真实同批离线对比：707字符回答的32条任务完整消息从9,987降到6,456字符（减少35.36%）；149批/4,181条合计从1,432,184降到1,117,959字符（减少21.94%）。142批变短，7个小批次因固定表格说明略增；不把字符节省冒充实际token、耗时或新模型质量改善。全部4,181条无损还原且与旧实际输入逐项相等。
- 校验：229项Skill离线测试通过，包括短ID解码、原始回复追溯、缓存重排、三轮子集映射稳定和服务重试冻结；76份历史已接受回复重新编码回放后保持1,826条观察、431条跳过完全一致，374个原任务/请求/接受文件哈希未变。对比与回放结果保存在 `data/result/high_trend/skill_optimization_20260908/compact_input/`；新增后端模型调用为0。独立ZIP在仓库外禁用第三方包后同样229项通过，43个分发文件逐字节一致；默认ZIP和2.3.0包已同步，2.2.0旧包保留。
- 按用户要求将趋势 Skill 升至 2.2.0：从 draft/review 契约、Prompt、模板、字段校验、预览编译器和最终 JSON/JSONL/Markdown 中移除 `validation_questions` /“下一轮验证”。将未入主卡的文章清单替换为用研主体的补充方向，新增代码分包、引用校验、按用户去重和严格过半筛选；全部选中趋势须逐条完成语义覆盖核对。主卡队列与用研补充完成状态分别标记。
- 已基于同一暂停预览更新 HTML、报告与结构化结果：配色与色彩表达至少 33/34 人、摄像头等功能部件外观构图至少 30/34 人、便携体量与握持体验至少 27/34 人；均为主题提及的保守下限，包含不同态度，不是偏好支持率。核对全部 5 条选中趋势未覆盖这些具体议题，保留反例及跨品类边界。主卡及补充共 104 条唯一引用逐字回查原输入通过；26 处图片关联中 15 处有文件，11 处为原资料未匹配编码，保留空路径，不猜补。原 73 个任务保持暂停，新增后端模型调用为 0。
- 验证：208 项包内测试通过；真实预览 1,826 条观察在代码小包中恰好覆盖一次（含完整问答），5 条趋势和 34 人分母一致；Skill 基础校验通过。默认分发包及 `design-trend-synthesizer-2.2.0.zip` 已同步，旧 2.1.0 包保留；提取规则、模板及内容键与旧版一致，继续复用经当前校验的用户提取缓存。
- 按用户最新要求暂停未完成批次，原运行保存为 `paused_by_user`，无在途调用；76/149 个提取批次已完成，其余 73 个保留续跑位置。基于 2,105 条已完成用户来源、1,826 条观察（34 位用户），并直接阅读本轮 5 条趋势源摘要，生成独立预览 `data/result/high_trend/20260908_preview_43ee1b4f/`：2 个通用设计机会，含 Markdown、HTML、JSON/JSONL、引用及图片路径、校验和用量报告。18 条唯一引用逐字回查原输入通过，10 个图片引用均有本地文件；未读取图片像素。预览新增后端 API 调用为 0，原运行累计已知 987,242 token，另有 24 次请求用量不完整，当前会话/子代理用量不可得。预览完成不等于原八阶段流程完成；原输入、任务、已接收结果及失败历史保留。
- 用户授权代码优先修复后已继续同一正式运行：新增严格有界 JSON 语法整理、历史完整记录组复用及仅缺口补齐，保留正式任务和原始尝试，不重置失败额度；聚合凭证逐记录追溯且不重复计 token。原先 3 个阻塞批次均已接收，63 条结果复用，8 条通过 3 次小请求补齐，实际新增 20,323 token；40 个原始来源/尝试文件哈希不变。包内 194 项测试通过，当前已完成至少 69/149 个提取批次，继续执行剩余流程，尚未形成最终趋势结论。实际补齐报告位于本轮 `host/record_repair_execution_report.json`。
- 已逐记录复核剩余 3 个阻塞提取批次的历史回复：71 条来源中 63 条存在完整合法记录组，8 条仍为正文改写、来源遗漏或观察/跳过冲突。复用清单与未解决原文保存在本轮 `host/record_repair_plan.json`，仅为待用计划，不代表局部结果已接收或补调用已执行；源数据、正式任务、原始回复和用量记录保持不变。
- 趋势 Skill 已增加确定性元数据整理：有效引文中不存在的图片编码直接移除，相同合法维度去重；保留模型原回复与差异追踪，禁止猜图号、改引文或立场。正式运行 `20260908T030351071993Z_43ee1b4f` 中两次校验失败的批次已从第二次原回复恢复，新增模型调用与 token 均为 0，历史失败记录及计数未改写。随后增加空回答纯 unclear 观察转 skipped 的窄规则，另一个两次失败批次同样零调用恢复；输入中 98 条问答的回答字段为空，不能用问题当证据。本轮已保存 61/149 个提取批次，3 个批次因正文改写或来源覆盖错误达到校验上限，另有 85 个可执行批次；为控制后续开销，已收齐在途结果并暂停等待追加调用决定，尚未生成趋势结论。已知 API 用量 826,134 token（含重试），10 次调用未返回完整用量；暂停详情位于本轮 host/pause_report.json。
- 另增唯一映射完整连续列表项的编号恢复规则，真实引文只补回“6. ”后整体校验通过，原回复不改写。另增纯代码逐观察诊断，第二次模型修复可一次看到最多 8 处错误，反馈有字符预算且不改原回复。再增完全相同的 skipped 声明去重，第三个真实失败批次零模型调用恢复，原尝试及失败额度保留。相关代码共通过 152 项包内测试，独立 ZIP 在仓库外禁用第三方包后同样全部通过；默认 ZIP 已更新，另存 `design-trend-synthesizer-2.1.0-normalization.zip`。27 个文件逐字节一致，原 2.1.0 ZIP 快照未覆盖；本次验证留痕位于 `data/result/high_trend/skill_optimization_20260908/normalization/`。
- 已将趋势 Skill 升级至 2.1.0，重点压缩首次运行开销：默认每批最多 32 条来源、64 条观察，按完整模型消息字符及预计输出量动态合批，不混不同用户、不截断原文；prepare/dry-run 增加逐批和汇总成本预览，字符不冒充 token。
- 提取只要求来源 ID、维度和态度，按需附连续引文、非默认字段和图片对象角色；短文本全文、默认字段、完整覆盖、图片编码与本地路径由代码恢复，已提取记录不再重复输出 coverage。原始紧凑回复保持不可改写，新的缓存契约与旧版隔离。
- 用户画像按用户去重；主题及后续阶段直接回看去重后的完整问答，避免短答脱离问题。相关维度可共用一个观察，反向态度、比较对象和条件仍保留；后续模型不重复接收逐字相同的 claim 和空解释字段。
- 同一真实日期范围（2026-07-07 至 2026-09-07）的代码对照：两版源索引哈希相同、4,182 条来源完整且恰好覆盖一次，2.0 的 541 个提取任务降至 149 个（减少 72.5%）；完整消息正文由 4,224,458 降至 1,432,723 字符（减少 66.1%）。未调用全量模型，不将请求字符或任务数减少解释为实际 token/耗时节省。可复跑的对照脚本及结果位于 `data/result/high_trend/skill_optimization_20260908/compact_v21/`。
- 验证：108 项包内测试与 13 项后端适配器测试通过，覆盖输入预算、上下文恢复、紧凑回复不可改写、缓存复用/失效、遗漏拒绝、长文引用和图片角色；Skill 基础校验通过。最终 ZIP 在仓库外、禁用第三方 site 包后全部 108 项再次通过；22 个包文件逐字节一致，默认 ZIP 与 2.1.0 分发包已同步，2.0/1.0 旧包保留。
- 独立 Agent 以 29 条真实来源完成八阶段 17 个语义任务，零接收失败，生成 1 张探索卡，保留 45 条观察、4 条显式跳过及 7 个图片路径；15 条省略引文准确恢复全文。此次为同一 Agent 上下文的语义验证，没有付费 API 或真实 token/延迟对照，不等同于多模型独立评审。另按同一用户原始顺序取前 32 条真实问答，默认预算下一批处理，一次接收成功：23 条有观察、9 条显式跳过，输出 27 条观察，保留多维和条件；全过程及额外单批验证已归档至 compact_v21/behavior_validation。

- 已将 `ref/design-trend-synthesizer` 升级至 2.0.0：提取使用多维观察、图片提及角色及按任务生成的模板/覆盖骨架，修复用户 `example` 示例和限额冲突；长回答按代码估计的观察预算缩小批次。
- 已加入逐用户源记录的内容缓存，核对原文、画像、模型参数、提取指令/契约版本和有效消息摘要；只复用已校验的提取结果，保留原始执行链，源指针与图片路径从当前输入重新关联。
- 流程扩展为八阶段，新增有完整成员覆盖的用户主题归纳与独立候选依据核对。候选分别提供趋势依据、用户依据、共同原则和应用假设；未代表/未配对/暂缓证据可追溯，后续反证仍完整覆盖候选维度。任务级 `source_records` 去重完整原文，多维不重复计人，比较图片不继承整句负面关系。
- 已提供标准库 `runner.py` 及项目后端 `backend/scripts/design_trend_model_adapter.py`：默认并发 2，分类有限重试、过载降并发/退避与连续拥堵暂停、认证停止、串行接收、进程锁和恢复；后端复用现有模型服务，不增加依赖或绑定 Skill 到本项目。
- 已加入 `performance_report.json` 和独立耗时统计入口。只读复核旧运行 758 次完成调用：提取中位 89.055 秒、累计占比约 93.1%；候选中位 56.731 秒。并发调用累计时间与真实时间跨度分开报告，未执行阶段和未知用量不记为零。统计及本次验证说明位于 `data/result/high_trend/skill_optimization_20260908/`。
- 验证完成：89 项包内测试、13 项后端适配器测试通过；新 ZIP 在仓库外、禁用第三方 site 包后全部 89 项通过，Skill 基础校验通过。独立子代理用小批真实资料完成八阶段 16 个语义任务和 19 项产物检查；未做新版全量付费模型回放，不能声称实际节省比例或完整独立模型会话验证。
- 默认分发 ZIP 及 `design-trend-synthesizer-2.0.0.zip` 已更新，21 个文件逐字节核对一致；保留 `design-trend-synthesizer-1.0.1.zip`。项目/个人技能链接继续指向规范目录，旧正式运行与源 JSON 未改写。


## 2026-09-07

- 最近两个月（2026-07-07 至 2026-09-07，含边界）的正式趋势归纳运行 `data/result/high_trend/20260907T095859449887Z_ac6c95e7` 已完成提取：6 条趋势、39 名有文本的用户，4,182 条来源记录逐条且仅覆盖一次，形成 8,080 条观察；拆分恢复后 570 个活跃提取任务全部接收。候选已接收 59/512 个任务，因持续服务过载及部分任务两次校验失败暂停派发，全部在途调用已保存，尚无正式趋势结论。续跑与失败详情见本轮 `host/pause_report.json`；真实样本支持的优化建议见 `host/optimization_notes.md`，正式 Skill 未在运行中改写。
- 已将通用设计趋势 Skill 升级至 1.0.1，代码默认将过程与最终 JSON / JSONL / Markdown 保存到项目 `data/result/high_trend/<run_id>/`，自动创建独立目录并返回 `run_dir` 供续跑；只读预览不创建文件，显式输出目录继续兼容。36 项测试通过，覆盖跨工作目录定位、完整结果保存、重复运行隔离及预览无副作用；同步更新协议、方案与可复制 ZIP。
- 已将通用设计趋势方案落地为自包含 Skill `ref/design-trend-synthesizer` 1.0.0，并链接项目/个人技能目录、生成可复制 ZIP。支持 `release_time` 闭区间与单端筛选、未知日期显式策略；Python 标准库负责分批、六阶段任务推进、引用/覆盖校验、去重统计、图片路径和 JSON/JSONL/Markdown 输出，模型由宿主按任务调用。
- 已实现未接收任务拆分、分轮候选收敛、完整源字段反证回查、单卡生成与审核；用户问句不能当回答，图片编码按完整边界匹配，暂缓/单侧/反证与路径缺失均可追溯。34 项测试及隔离复制、禁用第三方包的运行验证全部通过；独立 Agent 以真实小样本文本完成六阶段共 14 个语义任务。真实数据按 2026-01-01 至 2026-08-31 核对纳入 303 条趋势，生成并核验 576 个纯文本提取任务覆盖全部 4,479 条输入记录；未启动全量正式模型分析。
- 已完成通用设计高潜力趋势归纳方案 `docs/HIGH_POTENTIAL_TRENDS_PLAN.md`，按用户确认的不限定品类、纯文本输入与图片路径关联设计证据提取、双侧交集、反证、优先级及验收流程；已核对文本覆盖 39 名用户和趋势日期边界。当前为待评审方案，尚未实施模块或生成正式趋势报告。
- 已将趋势 `release_time` 统一为 `YYYY-MM-DD`，日期时间只截取源日历日期；同步提取脚本、JSON / JSONL、报告及说明。源表 500 条中 481 条日期已更新，19 条空值保留 `null`；全量核对其他字段及图片关联一致，15 项趋势整理测试通过。
- 已按用户要求排除 `ai_analysis` 包含“未提及”的整条问答，并同步提取脚本、现有 JSON / JSONL 和报告；共移除 43 条，剩余 3,511 条问答，70 名用户及图片关联保持一致。18 项用户整理测试通过，实际输出与按规则筛选后的原结果逐项一致，源快照保留用于追溯。
- 已新增用户调研离线整理脚本 `backend/scripts/prepare_user_research_data.py`，从指定 MySQL 库只读提取画像、问答、需求和有效图片反馈，凭据读取 `backend/.env` 的 `USRDB_NAME` / `USRDB_PASS`，不写入结果或日志。
- 已核实问答与需求分别通过 `transcend_model_a3v` / `transcend_model_a3w` 的 `source_bid`、`target_bid` 关联用户；同一只读一致性快照读取 70 名有效用户、3,554 条问答、621 条关联需求，另保留 44 条无法确定用户归属的有效需求。
- 已按用户确认的口径仅统计未删除用户的图片反馈：5,708 条反馈、1,480 个实际图片 URL，ENJOY 3,141 次、DISLIKE 2,567 次。不同源行重复计票保留追溯，重复关系与同源行重复 URL 不放大统计。
- 用户信息和图片信息输出为各自 JSON / JSONL，源快照、统计与异常报告、下载图片统一保存到 `data/userreseach_data/`；支持快照离线重放、图片校验、缓存复用和失败补下载。
- 已将需求中的图片编码限定为同一用户内匹配；当前 284 处编码未匹配到有效反馈图片，保留原文及未匹配状态。无归属需求与 18 条无效关系已明确记录，不推测归属或修改源库。
- 已定位 OSS 图片加速节点的间歇性 TLS 超时，增加可选 `--resolve HOST=IP` 连接级解析覆盖。仅使用已核实的 DNS 地址，保留原 URL、Host、SNI 和默认证书校验，不修改系统 DNS、VPN、代理或全局网络函数。
- 用户整理、趋势整理和 HTTPS 解析覆盖共 38 项自动化测试全部通过；全量用户归属、原字段、关系和逐图片计数已与源快照核对一致。运行说明见 `docs/USER_RESEARCH_DATA.md`。
- 已完成全部 1,480 张图片下载（约 620.33 MiB，失败及待处理均为 0）；逐文件核对 SHA-256、字节数、格式和实际宽高，并验证用户/需求图片路径、JSON 与 JSONL 内容、源快照哈希全部一致。中断下载遗留的临时文件已清理，图片目录与最终索引完全对应。

## 2026-09-06

- 已对多标签 Skill 脚本完成保持行为的模块拆分：编译入口分离字段与风格整理，语义校验分离主体/字段与风格/关系检查，包自检分离静态契约与独立回归测试。
- 已统一编译、结果校验和包检查中的知识库值域解析及单图视角规则；Schema、证据门槛、风格规则和模型参数保持原有契约。
- 保存脚本新增 `--compiled` 入口，后端编译一次后只派生预设、严格校验并保存，避免丢弃第二次编译报告及重复归一化。默认命令行入口继续支持原流程。
- 已将包内错误样例回归移入独立测试文件，并增加保存边界、有限 JSON 数值和值域/视角规则测试；包自检不再依赖源码字符串匹配，默认及子进程均禁止生成字节码。
- Skill 包版本更新至 1.6.4；独立临时包验证确认不依赖后端目录，默认保存与已编译保存的结果一致。
- 验证完成：74 项后端测试、12 项包内测试、完整包校验和 Skill 基础检查全部通过；与重构前版本逐项对照的 12 组编译结果/报告及 21 组校验错误/警告完全一致，临时独立包默认自检未新增或修改任何包内文件。

## 2026-09-05

- 已新增后端离线脚本 `backend/scripts/prepare_trend_data.py`，从文章信息表按原 ID 提取指定 13 个趋势描述字段；标签、分类和视觉信息按确定性规则整理成适合 LLM 读取的结构。
- 输出统一保存到 `data/trend_data/`：完整 JSON、逐趋势 JSONL、按趋势关联的原图、下载报告和源表副本；保留来源行号、源表哈希、逐图实际尺寸、文件哈希及下载信息。
- 已实现 `||` 多图拆分、实际图像校验、有限重试、有效缓存复用、每 100 张保存索引及只读 `--dry-run`；修复 MaterialDistrict 缩放接口限流及解包后非 ASCII 文件名编码问题。
- 已增加 15 项离线自动化测试，覆盖源字段保真、空值/重复 ID、图片顺序与归属、无效图片、HTTP 失败、缓存恢复、完整导出、原图地址追溯和 Unicode URL，全部通过。
- 已核对源表全部 500 条趋势、13 个字段和 4,081 个图片引用，确认值、ID、原始链接和顺序一致；原始表副本与 SHA-256 一致。
- 已完成全量整理和重复运行验证：4,080 张图片成功下载并复用，1 张 Gucci 媒体图在延长至 60 秒并重试后仍超时，失败 ID 为 `11730_034`，已保留原 URL 和空本地路径供后续补下载。
- 运行方式与输出结构见 `docs/TREND_DATA.md`；生成的本地趋势数据已加入 Git 忽略规则。

## 2026-08-31

- 已根据 `AGENTS.md` 创建前端、后端及测试代码的主要目录结构。
- 后端各职责目录已初始化为 Python 包；前端职责目录与测试目录已添加版本控制占位文件。
- 已建立集中式大模型目录，配置 6 个可用模型及对应的 `model_provider`。
- 已实现基于 `LLM_KEY`、`LLM_URL` 的统一模型创建、同步调用和流式调用模块。
- 已提供 `GET /api/v1/llm/models`，供后续前端读取可选模型列表。
- 已补充模型目录、调用适配及列表 API 的自动化测试。
- 已完成 `claude-opus-5-20260820` 与 `gpt-5.6-terra-20260820` 的真实推理验证。
- 已修复 OpenAI 兼容 provider 缺少 `/v1` 路径导致的 404，并增加路径防重复测试。
- 已完成当前项目代码、设计 DNA Skill 参考资料及工程配置的首次 Git 提交准备。
- 已完成 Vue 3 Web 主页面和设计 DNA 工作台，包含单张、批量、文件夹上传、图片多选、用户要求输入、模型选择和确认步骤。
- 已实现图片文件存储、预览、批量提取任务、任务状态轮询、成功结果列表及业务/详情双视图 API；当前阶段不写入 SQL 数据库。
- 已用 DeepAgents 原生 Skill 加载机制接入 `multimodal-design-dna-extractor`，任务内每张图片独立运行一次单图 Skill。
- 已接入 Skill 的 JSON Schema、语义校验、标准结果落盘和业务视图转换脚本；可确定的低置信字段计数由应用层重算。
- 已为每次成功提取保存独立追溯元数据，记录输入图片哈希、模型、用户要求、Prompt 版本、Skill/Schema/知识库版本和执行时间。
- 已修复当前 DeepAgents 版本要求 `FilesystemPermission` 对象而非字典规则的兼容问题。
- 已为长推理开启流式模型响应，并兼容 Anthropic 网关将 `context_management` 流事件返回为字典的情况。
- 已用真实手机图片验证 Claude Opus 5 与 GPT-5.6 Terra 的文本加图片多模态识别。
- 已用 GPT-5.6 Terra 完成一次完整 DeepAgents + Skill 提取，生成并校验完整结果和业务视图。
- 已用 Claude Opus 5 从浏览器确认入口完成一次完整 DeepAgents + Skill 提取，结果、业务视图与追溯元数据均正确生成。
- 已将 Claude Opus 5 与 Claude Fable 5 的输出上限设置为各自官方最大值 128K token；其他模型保持不传 `max_tokens`。
- 已将等价但标点不同的风格名称确定性还原为知识库标题原名，Claude 结果最终通过校验且无 warning。
- 后端 20 项自动化测试全部通过，前端 TypeScript 检查与 Vite 生产构建通过。
- 已在真实浏览器中验证单张、批量和文件夹上传、图片预览、6 模型下拉列表、任务确认，以及业务/详情 JSON 切换。
- 已新增根目录 `start.sh` 一键启动脚本，统一使用 conda `base` 环境启动后端并同时启动前端，支持信号清理与自定义端口；已实测前端、后端、API 代理及 `Ctrl+C` 双进程清理均正常。
- 已将业务视图和详情视图从原始 JSON 文本改为结构化字段展示：业务视图包含设计摘要、风格、关键 DNA、语义坐标、不确定项、新候选和质量卡片，详情视图按目标物、图片质量、模块适用性、风格规则、设计元素、证据与质量分组呈现。
- 已增加通用的结果字段标签与递归展示能力，确保后续 Schema 新增字段不会静默漏显；已用真实提取结果验证桌面端与 390px 移动端布局，页面无横向溢出和运行错误。
- 已实现素材和提取结果的删除 API 与前端二次确认入口；运行中素材禁止删除，素材删除不会连带清理历史结果。
- 已为提取结果增加独立缩略图及旧结果按图片哈希补齐机制，结果列表现以真实图片缩略图展示；相关后端自动化测试增至 24 项并全部通过。
- 业务视图升级为 `design_dna_business_view_v1.1`，新增 `model_id` 生成模型字段；新结果直接写入所选模型，已有结果从追溯元数据自动补齐并在业务视图摘要区展示。
- 已对 `20260831-213714.jpeg` 完成单主物品跨品类设计 DNA 提取，完整结果通过 v3.1 Schema 与语义校验，并生成对应业务视图。
- 已将 `multimodal-design-dna-extractor` 更新至 `1.1.1`、知识库更新至 `2.1`，收紧 `KineticEnergy` 与 `SaturatedBold` 的运动锚点及冲突仲裁边界，并用红色泡罩微型车样例完成规则回归。
- 已将 Skill 更新至 `1.2.0`、知识库更新至 `2.2`，新增可选“异混淆特征”字段，将 `KineticEnergy` 与 `SaturatedBold` 的共享表象和关键分界点集中纳入该字段，并同步更新提取协议、输出约束、索引与评测规则。

## 2026-09-01

- 已以现有原 Skill 为基线完整复制并独立建立 `multimodal-design-dna-multitag-extractor` 1.0.0；复制后原 `multimodal-design-dna-extractor` 目录保持不变。
- 新 Skill 使用 Schema `design_dna_multitag_extraction_v1.0` 与知识库 4.0，输出 0～3 个已确认同层标签，取消一二级及主次风格字段，并增加候选状态、dominance、作用区域、两两关系仲裁与组合摘要。
- 新本体含 30 个活动风格标签、7 个后台 facet、32 对显式关系和 1 条复合依赖；29 个 atomic 标签独立计权，`MysticOrganic` 仅作零权重复合解释，`TribeIdentity` 已迁移到 `IDG-05/07/09` 且不再作为风格。
- 新 Skill 已增加可共存双标签完整样例及 48 条评测，其中 5 条为明确多标签触发例；确定性校验器可拒绝旧层级字段、废弃标签、dominance 不闭合、候选状态不一致以及缺失/重复关系仲裁。
- 新 Skill 的规范 DNA 已增至 184 个字段；新增 `DET-17 细节语法角色`，NeoRetro 必须由两族独占证据及 `DET-17=历史造型化` 闭环，普通泡罩、圆灯、轮圈、现代 Logo 或普通控件不能替代该门槛。
- conditional 同区关系现显式区分 `forbidden` 与 `independent_evidence`；后者只有在双方核心字段、核心证据和仲裁引用均可独立追溯时才允许共存，confirmed 标签引用的全部 core/aux 字段同时要求置信度不低于 0.75。
- 已在无历史结论上下文下完成新 Skill 独立前向盲测：红色泡罩微型车确认 `SaturatedBold + RefinedMinimalism`，二者由色彩与克制边界细节的独立证据共存；`NeoRetro`、`KineticEnergy` 与 `BiomorphicForm` 均被拒绝，正式结果首次严格校验即通过。
- 已将根目录 `start.sh` 的运行环境从 conda `base` 修正为 `314`，并在激活环境后校验 `pnpm` 与后端应用依赖。
- 已在 `314` 安装 Python 后端依赖、Node.js 22、`pnpm 11.19.0` 和测试工具，并按锁文件安装前端依赖；后端 29 项测试、前端生产构建、前后端启动、API 代理及退出清理均验证通过。
- 已将 Cloudflare Quick Tunnel 纳入 `start.sh`，启动时自动输出动态公网域名；前端限制放行 `*.trycloudflare.com` Host，后端同步允许该来源的 CORS 请求，公网首页和 API 均已实测可访问。
- 已将 `multimodal-design-dna-extractor` 升级至 `2.0.0`、Schema `v4.0`、知识库 `3.0`；一级标签收敛为 6 个导航族，31 个活动二级风格使用稳定 ID、显示名、别名和迁移关系。
- 已合并重复的静奢/精致克制标签，拆分自然表面与仿生形态，并为全部风格统一硬门槛、决定锚点、独立辅助证据、硬排除及 8 组集中混淆仲裁。
- 已将 DNA 收敛为 183 个规范字段、25 个真别名与 31 个零权重兼容推导，原六维改为宿主兼容投影；新增风格、字段注册表及稳定索引生成校验。
- 已将适用性、可观察性、证据模式与计算状态分离；当前单图流程不激活 M15 参考/趋势 profile，材料、工艺和随角变化只保留视觉候选或不可计算状态。
- 已同步更新 DeepAgent、业务视图、前端字段展示、v4 示例、校验器和回归评测，结果保存继续由应用层负责。
- 已通过红色泡罩微型车独立前向盲测发现并修复“通用圆灯/轮毂/结构包边误触发复古风格”的跨组冲突；`NeoRetro` 改为“复古重组”，并要求表达性复古线索不可全由基本功能解释。
- 已补强确定性校验：风格命中须引用已确认规范字段，required 色彩不可跳过，非直接字段不得滥用 `not_requested`，旧六维兼容槽不得由模型生成。
- 已进一步执行 profile、必要视角、风格决定/辅助 allowlist、枚举与数值值域、M15 排除及新枚举候选约束，并将通用 `none` 改为类型内缺席值。
- 已将纯字符串标签集合与结构化 list 分离，补齐斜杠值域解析、派生依赖/源证据校验，并禁止单图激活不可表达的 multi-face profile。
- 已修复 v4 业务视图的中英标签标准化及 M04/M10 分组语义，同时保留 v3 兼容路径和回归测试。
- 已通过终审规则下的独立前向盲测：红色泡罩微型车稳定判为 `SaturatedBold`、无次风格，且不再误触发 `KineticEnergy`、`NeoRetro` 或 `BiomorphicForm`。
- 已调整版本控制规则，排除本地 PPTX 演示文件。
- 已将独立 `multimodal-design-dna-multitag-extractor` 升级至 1.1.0、知识库升级至 4.1；新增玩趣构成、角色拟态、气室膨胀、透明层叠、解构拼装、数字故障、粗野原块和制作式不规整 8 个原子风格。
- 已新增 8 个直接可观察 DNA 字段、22 对显式关系及 12 个只用于查询的命名组合预设；组合预设不进入提取结果，也不参与反向补证。
- 扩展 Skill 现含 38 个活动标签、192 个规范字段、54 对显式关系和 80 条评测；流程、Schema、索引、Prompt bundle、示例与确定性包校验均已同步。

## 2026-09-02

- 已将独立多标签 Skill 升级至 1.2.0、输出 Schema 升级至 `design_dna_multitag_extraction_v1.1`，最终结果新增 `derived_style_presets`。
- 已拆分模型阶段与最终结果 Schema；模型 Prompt 不加载组合预设规则，Python 保存流程仅根据 confirmed 原子标签确定性覆盖写入组合预设。
- 已增加 14 条组合派生评测、伪造值拒绝门禁和保存链路验证；收紧玩趣组合，避免单独鲜彩误触发。
- 已将当前 DNA 提取模块的 DeepAgent 从原单标签 Skill 切换并约束到 `multimodal-design-dna-multitag-extractor`；Agent 只生成模型阶段 JSON，应用层调用新 Skill 保存脚本完成组合派生、最终校验和落盘。
- 业务视图升级为 `design_dna_multitag_business_view_v1.0`，以同层 `tags` 展示已确认原子标签，并包含 dominance、作用区域、确定性组合预设、组合说明和标签两两仲裁；模型、来源 Schema 与知识库版本继续可追溯。
- 结果列表 API 与前端摘要已由 `primary_style` 改为 `style_tags`，同时保留已有单标签历史结果的列表读取兼容。
- 业务视图和完整详情页面已移除主/次、一/二级风格布局，改为同层标签卡片、完整候选排名、派生组合和关系仲裁；对象型 DNA 值使用分层字段卡片展示，不再转成 JSON 字符串。
- 多标签 Skill 包及示例、业务视图转换、后端 API/存储/Agent 共 28 项自动化测试全部通过；前端 TypeScript 检查和生产构建通过，并用双标签手机样例完成浏览器业务/详情双视图验证。
- 已将多标签 Skill 升级至 1.3.0，模型阶段改用 `design_dna_multitag_observation_v1` 精简观察协议；最终 `design_dna_multitag_extraction_v1.1` 与前端展示协议保持不变。
- 已新增确定性结果编译器，从字段、风格和关系注册表补全静态元数据、模块结构、统计、dominance 归一化、排序、confirmed 候选镜像、不确定字段镜像及可推导证据闭环。
- 校验失败现按确定性/语义错误分类；确定性错误不再调用模型，语义错误先请求 JSON Pointer 局部补丁，仅在补丁无效时进行一次完整观察结果兜底。
- 已新增 DeepAgent 内部模型请求数、成功/失败次数、Token 用量、Agent 执行次数、网络断流重试、编译修正和语义修复追踪；流开始后的瞬时断连由应用层最多重试一次，429 限流不立即重试。
- 已新增覆盖全部活动风格、规范字段类型和标签关系的模型精简参考包，并将稳定 Skill 上下文预装为可缓存系统前缀，减少逐文件读取引起的重复模型调用。
- 双标签样例的模型输出由约 16.3 KB 降至约 8.9 KB，减少约 45%；精简观察编译后继续通过完整 Schema 与语义校验。
- 已用 Claude Opus 5 对 `folder-phone-a.png` 完成真实多模态贯通，生成 `20260902_020814_folder-phone-a_design_dna.json` 及业务视图；结果含 69 个字段、4 个不确定项和 `NordicCalm` 标签，最终严格校验通过。
- 已将多标签 Skill 升级至 1.4.0；DeepAgent 新增按视角与活动 profile 的字段准入工具，只提取可观察、风格需要或用户关注的 DNA，避免穷举 M13/M14。
- 确定性编译器已覆盖 profile/必要视角过滤、字段值别名、关系词提取、离散序数量化和低置信不确定项镜像重建；无法无损整理的值保守转为未知。
- 风格字段命中现按 decisive/auxiliary allowlist 重新归类；无可用字段、缺区域证据或规则闭环不足的 confirmed 标签自动降为 provisional，并重建候选、排序和组合摘要。
- 校验问题已结构化并关联模型观察 JSON Pointer；失败结果写入独立诊断追踪，任务 API 与前端按字段规则、风格证据等类别展示完整错误，不再只返回截断文本。
- 已把 `20260901-165929.jpg` 与 `20260829-163949.jpg` 暴露的两类错误固化为回归场景；后端 44 项测试通过，前端 TypeScript 检查和生产构建通过。任务去重按约定未实施。
- 已使用 Claude Opus 5 真实复测两张历史图片并生成有效结果：第一张未使用任何语义修复，第二张的风格错误由宿主整理后通过；复测额外发现并修复了“高置信观察误放入 uncertainties 会消耗一次模型补丁”的问题。
- 已将多标签 Skill 升级至 1.5.0，风格处理改为“裁剪额外弱引用后重新闭环”；弱字段继续保留在 DNA/不确定项中，不再因单条多余引用整体降级已满足规则的风格。
- 已对 NeoRetro 的独立线索族与 `DET-17=历史造型化` 增加宿主硬门槛复核，确保提高风格召回时不放宽特殊规则；后端回归增至 46 项。
- 已将多标签 Skill 升级至 1.6.0，新增宿主专用值级证据规则；SaturatedBold 可由“高彩度中心 + 大面积主色”自动建立决定证据，并由 `CMP-13` 的色相/彩度对比自动建立辅助证据。
- confirmed 主模型输出已将规则计数和 core/auxiliary 命中数组改为可省略，DeepAgent Prompt 明确不再生成这些机械字段；值级规则无法安全判断时，只调用一次不含图片和完整 Skill 的窄范围语义复核 Agent。
- 语义复核现支持一个字段或最小字段组合，core 必须包含 `decision_use=hard` 字段；复核结果仍经过完整编译与严格校验，失败或低置信时保持未分类，不按候选排名强制确认。
- 已用 GPT-5.6 Terra 实测 RefinedMinimalism 窄范围复核：1 次模型请求、3130 输入 token、431 输出 token，补挂 `DEV-03+DEV-05` 决定组合及 `CMF-07` 辅助证据后恢复 confirmed，最终校验通过。
- 已用 `20260829-163949.jpg` 的既有字段重放原 SaturatedBold 缺辅助证据场景；`CMP-13=[色相,明度,材质]` 被确定性挂为辅助证据，不调用语义复核即可恢复 confirmed 并通过最终严格校验。
- Skill 包完整校验、模型 Schema 新鲜度检查及后端 49 项自动化测试全部通过；任务去重仍按约定未实施。
- 提取任务 API 已增加逐图片阶段、阶段估算进度和最近 80 条受控执行事件；覆盖准备、主模型请求、解析、确定性编译、风格语义复核、局部修复、最终校验、业务视图和结果保存。
- 前端提取卡片新增实时执行记录时间线，轮询间隔调整为约 1.2 秒；网络超时与规则校验失败使用不同提示，不再统一显示为“未通过最终检查”。
- 过程事件只展示应用阶段和模型请求生命周期，不输出模型思维内容；后端回归增至 51 项，前端 TypeScript 检查和生产构建通过。
- 已将多标签 Skill 升级至 1.6.1；有效证据已明确区域但主体可见区域清单漏登记时，由宿主确定性同步声明并保留证据源路径，不再触发整轮模型修复。
- 诊断 `20260902_085729_432730_725e648a` 的原模型观察已重放通过最终严格校验；前端诊断标题与错误正文不再重复展示同一句内容。
- 后端 54 项自动化测试、Skill 包规范与完整性校验、前端 TypeScript 检查和生产构建均已通过。
- 已将多标签 Skill 升级至 1.6.2；最终模块路径现可映射回精简观察，局部补丁无效时不再覆盖原始问题，低置信推断字段可被保守移除而不阻断其他结果保存。
- 诊断 `20260902_094823_169638_5b82cd4e` 的 `IMG-08` 单证据场景已固化为回归；重放时发现的两处不超过 0.02 的证据框取整越界也已由受限宿主规则处理，后端回归增至 57 项。
- 已新增根目录 `stop.sh`，可按默认或自定义端口安全停止本项目的前后端服务。
- `start.sh` 现会等待后端 OpenAPI 就绪后再启动前端，消除首次加载时前端代理请求早于后端监听的瞬时错误。
- 已将多标签 Skill 升级至 1.6.3；宿主可在值域校验后展开合法 enum 的单键 `label` 包装，并将证据框近邻取整容差收敛为 0.05。诊断 `20260902_125932_756377_781f3477` 的 18 个枚举类型错误及随后暴露的主体框边界错误均已通过无模型重放闭合。
- 已用 design-trend-synthesizer 3.0 轻量流程完成一轮真实归纳：趋势窗口 2026-07-08~2026-09-08（纳入 5 条）、用研取 `users.json` 前 20 位用户，模型 gpt-5.6-sol-20260820；7 次适配器调用（6 包方向笔记 + 1 次综合），输入 163,854 / 输出 9,198 token，状态 complete，产物在 `data/result/high_trend/20260908T153902_lean_ddf23615/`。

## 2026-09-09

- design-trend-synthesizer 3.0 轻量结果新增 `image_paths.md`：分别列出最终结果关联图片，以及从全部入选用户来源中提取并与第一部分去重后的图片路径；两部分均按规范化绝对路径去重，不增加模型调用或图片读取。
