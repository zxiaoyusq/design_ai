# 已知问题

## 2026-09-21：原始全量用户 JSON 存在格式错误

- 盘点图片来源时，`data/userreseach_data/users.json` 无法被标准 JSON 解析器读取，报 `Expecting value: line 823 column 9 (char 28418)`。本轮未修改或尝试猜测修复该源文件。
- 全目录其他图库仍按实际文件收录图片，能从 `images.json`、精选用户等有效文件补齐的来源照常记录；剩余文件保留路径和文件指纹，不能承诺全部用研记录关联完整。错误已保存在其他图库索引的 warnings，并在网页档案详情中可查。

## 2026-09-21：高潜趋势的需求图片未复用同用户正向偏好关系

- 状态：已定位，尚未修改关联规则。`lean_input.build_sources` 仅为 `user_qa` 按回答图号查找同用户 LIKE/ENJOY 偏好；`user_demand` 仅走显式链接，且要求链接自身带正向标签。相同图片的正向标签在偏好表、路径在需求链接时，需求来源可能丢失图片。
- 实例：任务 `web_85176208a42140fd85b577116795cbba` 的 `U000113`（用户 75、需求 763）正文提到 E324；原始需求链接有本地路径但 `emotion_tag=null`，同用户 `image_preferences` 中 E324 为 ENJOY 且有路径，冻结来源的 `image_refs` 却为空。
- 边界：该回答同时讨论 E324 的简洁图案和 P259 的折叠形态，当前卡片引用的是折叠观点。修复来源层的漏联不代表 E324 应直接补进折叠方向；方向图片仍需对应具体观点，不能将同记录或同用户全部喜欢图片挂入卡片。
- 本轮结果 14 个方向中 10 个无用研图片，53 条核心用研来源仅 5 条带正向图片。另有纯文字回答、无本地路径和 DISLIKE 过滤等原因，不能将所有空图库归因于此缺陷。已关联的 410 处图片引用均有本地文件。

## 2026-09-14：项目固定 pnpm 版本与服务器 Node.js 版本不兼容

- `frontend/package.json` 固定 `pnpm@11.19.0`，但当前服务器为 Node.js 20.20.0；执行 `pnpm build` 会因缺少 `node:sqlite` 失败，并提示 pnpm 至少需要 Node.js 22.13。
- 本轮直接使用现有 `node_modules/.bin` 完成 Vue TypeScript 检查和 Vite 生产构建，证明本次前端文案修改可编译；但 `start.sh` / `site.sh` 仍通过 pnpm 启动，服务下次重启前应升级 Node.js，或另行确认并固定兼容 Node 20 的 pnpm 版本。

## 2026-09-10：`tests.test_llm_api` 的 TestClient 请求超时

- 在当前 Python 3.14 / `314` 环境执行 `python -m unittest tests.test_llm_api -v` 时，首个 `TestClient(app).get("/api/v1/llm/models")` 超过 90 秒未返回；该问题未由本次 CORS 变更引入，仍需单独定位 TestClient 或其运行时依赖。
- 实际部署中的 `http://gamedevcenter.ahagamecenter.com/api/v1/llm/models` 已返回 200 和完整模型列表，因此当前不影响公网服务；启动脚本的实际启停与前端生产构建均已通过。

> 2026-09-08：趋势Skill 3.0已取消旧八阶段默认路径，以下2.x证据分配、编号修复与严格筛选问题保留为历史记录，不再通过新任务的模型反复纠错处理。新流程不承诺全量语义覆盖；未知引用只留提示，不伪造来源。

## 2026-09-08：候选生成的证据编号混用与抄写错误

- 状态：前 20 用户实测已复现，通用自动恢复规则尚未覆盖后续阶段。`propose-52966e6e8e4518e87408` 把同一服装偏好同时写为正确证据 ID 和来源式编号，导致未知 ID 与重复分配；`propose-640f6cc412e7f649c2e3` 在依据字段拼接了两条证据的长 ID。
- 本轮处理：后一例通过代码比对同任务历史回复，确认候选成员集合与依据文字完全相同，且只有一个合法历史 ID；仅恢复该编号，额外模型调用为 0，候选语义仍交由后续独立 screen 核对。所有原始回复、失败次数和已发生用量保留。前一例的代码探针验证了同类暂缓项保留全部理由后可以合并，但实际第二次后端调用已完成，不追溯扣减其成本或覆盖已接收回复。
- 证据：本轮目录下 `propose_record_id_confusion_case.json`、`code_recovery/propose-640f6cc412e7f649c2e3.execution.json`。
- 后续候选：把冻结短 ID 扩展至主题、候选和反证阶段；依据关系与唯一历史映射可由代码整理。多观察共用一个来源、不同立场冲突或没有历史依据时不能猜选编号。

## 2026-09-08：趋势主题归纳的证据分配不稳定

- 状态：前 20 用户实测已复现，通用流程尚未修复。主题回复出现漏分配、跨主题重复分配、长证据 ID 抄写错误及超出本组证据的维度；校验器会拒绝，不会静默推进。
- 原因边界：提取观察可能包含多个对象和设计观点，主题契约又要求每条观察恰好归入一个主题或暂缓。模型需要同时完成语义分组和排他 ID 清单维护；这与服务端过载是不同问题，不能归结为输入长度。
- 本轮处理：先核查历史完整合法主题组能否组合覆盖输入。两例虽然存在结构合法的组合，独立语义复核仍发现组外事实、题干举例被当作用户采纳、重要反对条件丢失，因此拒绝机械拼接；不能以删除重复 ID 掩盖这些问题。无可用代码恢复的批次另记一次有界独立宿主语义处理，不重置后端失败次数或冒充其缓存。原执行凭证缺少单任务用量，后续从本地逐响应日志按当前 root_turn_id 与 response_id 去重，另列宿主/子代理真实总用量及缓存输入，不能把它混入后端单次调用统计。
- 证据：`data/result/high_trend/20260908T092310535168Z_72de0f3e/` 下的 `theme_recovery_review.json`、`theme_78339_semantic_candidate_review.json`、`remaining_theme_group_recovery_probe.json` 保留结构和语义核对。
- 后续候选：短 ID 与共享上下文、逐证据输出主题编号后由代码汇总清单、有界多错误反馈；同时评估多点观察的可追溯拆分或有界跨主题复用。需另测覆盖质量，不能仅凭减少批次数宣称改进。

## 2026-09-08：候选筛选仍可能接受缺少双侧依据的推断

- 状态：未解决，前 20 用户实测中的特定风险样本已确认一例，不能据此估计全量正确率。
- `c-2432afb19b086ba734c942b6` 的正式 screen 接受“按部位控制金属的使用强度”，但趋势仅说明建筑中混合使用金属和其他材料，未表达金属视觉强度或审美接受度；不同用户的金属机身、边框意见也不能证明同一用户的偏好随部位变化。
- 当前处理：完整原文与旁路核对留在本轮 `screen_risk_sample_review.json`，不修改冻结任务或接收回执；后续合并、反证与成卡仍需检查是否保留此推断。结构和引用合法不等于双侧共同原则成立。

## 2026-09-08：趋势证据的记录覆盖不能证明观点完整

- 状态：未解决，已在前 20 用户实测中确认一个真实案例；并未进行全部记录的语义遗漏率测量。
- 影响范围：本轮复用的 2.1.0 缓存中，`user:52:user_qa:635` 原回答有连续编号 1–34 的 34 个条目，实际观察只引用前 5 项。第 25 项关于镜头过小影响质量感知的观点未进入该记录的观察，但当前契约重新校验仍通过。不能将其归因于 2.3.0 本轮新调用。
- 原因：现有代码核验连续原文、合法属性和来源记录是否出现；一条记录只要有一条合法观察，即可记为已提取，不能据此证明记录内部所有设计观点都已覆盖。34 个条目也不等于已证明有 34 个独立诉求。
- 当前处理：保留原缓存及来源留痕，不将颜色观察当镜头依据；用研补充中的镜头观点引用同一用户另一条已提取需求 `user:52:user_demand:139` 的真实连续原文。案例位于 `data/result/high_trend/20260908T092310535168Z_72de0f3e/semantic_coverage_case.json`。
- 后续候选：对明确编号的长回答先由代码生成带原文偏移的子片段，分别校验处理覆盖；未覆盖片段只标为疑点，由语义核对判断相关性。缓存也应带内容覆盖质量标记，不能仅因引用合法而声明全部观点完整。

## 2026-09-08：完整趋势运行的服务失败留待续跑

- 原运行 `20260908T030351071993Z_43ee1b4f` 在用户要求暂停时保留两项服务阻塞：`extract-8779c3f106a71eaf999d` 累计三次过载；`extract-8b2092bbb9b425e78785` 返回 `APIError: Upstream request failed`，按永久服务错误记录。原始尝试与失败额度未改写。
- 当前状态为 `paused_by_user`，无在途调用，不再重试这些批次。用户要求的部分数据预览已在独立目录生成；不把该预览视为完整运行恢复或服务错误已解决。

## 2026-09-08：趋势提取回复的正文改写与来源覆盖错误

- 状态：上述 3 个阻塞批次已恢复。用户授权后增加代码逐记录复用与有限缺口补齐，63 条合法记录保留，8 条在 3 次仅含缺口的模型请求中补齐，全量严格校验通过。正式运行 `20260908T030351071993Z_43ee1b4f` 已继续；这并不代表后续趋势结论已完成。
- 影响范围：需求来源 `user:55:user_demand:153`、`user:84:user_demand:686/685/684/683/680` 的回复引文改写或拼接了正文；问答 `user:52:user_qa:599` 在两次回复中均遗漏，`user:52:user_qa:588` 同时出现观察与跳过声明。不能靠删除冲突判断、猜补遗漏或替换全文掩盖错误。
- 当前处理：图片编码、相同维度、完全相同的跳过项、空回答以及可唯一恢复的连续列表编号已由确定性规则处理；3 个其他历史失败批次已零模型调用恢复，原始回复和失败额度保留。逐记录检查发现这 3 个阻塞批次中有 63/71 条完整结果可留待复用，8 条没有合法历史结果；局部结果尚未接收。
- 后续处理：同类格式和元数据问题优先代码处理，内容问题只补齐剩余原记录；不清空失败计数或把局部覆盖冒充整批通过。实际恢复及用量见本轮 `host/record_repair_execution_report.json`。早先暂停快照保留为历史记录，最新状态以 runner 状态及 performance_report 为准。

## 2026-09-07：趋势 Skill 的固定提取示例与用户任务立场枚举不匹配

- 状态：2026-09-08 已在 Skill 2.0.0 修复。模板按当前任务类型生成立场示例与逐记录覆盖骨架，实际观察上限统一为 24；新增契约与完整流程回归。原 1.0.1 运行保留原样，不追溯改写旧结果。

- 影响范围：`design-trend-synthesizer` 1.0.1 的 `contracts.response_template("extract")` 固定展示 `stance="example"`，而用户问答和需求禁止此值。正式运行中的多个纯用户任务曾返回该值；模板不匹配已核实，但不能据此认定每次误用都由模板直接导致。
- 当前处理：校验器拒绝无效回复，独立任务继续执行；同一任务两次失败后保留原始请求、回复及错误，不跳过来源或修改已接收结果。本轮详情见 `data/result/high_trend/20260907T095859449887Z_ac6c95e7/host/optimization_notes.md`。
- 后续处理：在新版本中按任务输入类型生成示例和合法枚举，并直接展示本任务实际输出限额。保留来源与态度校验，不能把无效 `example` 自动改为用户支持。

## 2026-09-07：44 条用户需求缺少用户关联

- 影响范围：`transcend_model_iduserdemands` 中数据库 ID 904–947 的 44 条未删除需求，在当前 `transcend_model_a3w` 中没有关联记录，无法确定归属用户。
- 当前处理：原字段和源 ID/BID 保留在 `data/userreseach_data/users.json` 根节点的 `unlinked_demand_research`，标记 `link_status=no_active_user_relation`。不删除记录、不推测用户，也不改写数据库。
- 后续处理：由业务侧确认归属并补充源关系后重新提取。源关系中另有 18 条指向不存在用户和已删除需求的记录，已过滤并记入 `export_report.json` 的 `ignored_relation_count`。

## 2026-09-05：一张趋势来源图片暂时无法下载

- 影响范围：趋势 `11730` 的第 34 张图片，`image_id=11730_034`，来自 `media.gucci.com` 的 `salone_KV_4000x2250.jpg`。
- 表现：图片 GET 请求持续读取超时；将单次超时从 30 秒延长至 60 秒、每轮额外重试 2 次后仍失败，另一个 HTTP 客户端也出现 HTTP/2 传输错误或超时。
- 当前处理：其余 4,080 个图片引用已全部下载。完整趋势描述和失败图片原始 URL 均保留在 `data/trend_data/trends.json`，失败项 `local_path=null`；明细见 `data/trend_data/download_report.json`。
- 后续处理：外部图源恢复后重新运行 `backend/scripts/prepare_trend_data.py`，脚本会复用有效本地图片并补下载失败项。未猜测或替换成其他图片。

## 2026-09-09：高潜趋势模型流读取错误

- web_b8fa48051b704d85907aad1b20cf3c19的digest-002在约140.49秒后收到openai.APIError: stream_read_error；实际消息47,654字符，未触发48,000字符预算。上游网关/模型端的具体原因尚未确认，不将其断言为本地超时或上下文溢出。
- 已补齐可公开错误持久化及手动继续入口，保留已完成笔记和历史用量；这改善可诊断性与恢复方式，不保证上游错误不再发生。未重放真实请求。

- 页面验收继续入口时发现原任务文字要求“只选择前20个用户”，但网页参数尚无user_limit，任务counts.selected_users=70、users_with_text=39。自然语言要求并未转成代码范围筛选；继续功能保持冻结输入不变，不能声称该任务只分析了20位用户。2026-09-09 已为新任务补齐用户范围选项及整理资料前的代码解析，预估和详情展示实际数量；旧任务仍为 70 位，若需前 20 位应新建任务，恢复不会更改旧范围。


## 2026-09-09：高潜趋势固定输出预算导致用研正文缺失

- web_33a53ce68fa1410f8aadc0288e8e7123 使用 qwen3.7-max，六个用研批次中五个达到 2,500 输出 token，finish_reason=length；四个无可用正文，一个正文截断。结果标为 partial，旧结果保留。缺失正文与模型内部推理的关系缺少明细，未进一步推断。
- 已取消网页与 Skill 新任务的默认输出上限，并保留截断提示；仅离线验证参数不再传入，未重放该研究，不能据此声称旧任务的内容缺口已补齐。

## 2026-09-21：旧版用户研究 JSON 无法解析

- 本机 `data/userreseach_data/users.json` 存在既有 JSON 语法问题：第 823 行第 9 列（字符 28418），导致原始数据集的目录读取返回 400。未推测修复或覆盖该文件。
- 本次全量研究使用的 `users_selected_5.json` 可正常解析，已完整执行；新版数据集及已保存结果的展示不依赖上述旧文件。
